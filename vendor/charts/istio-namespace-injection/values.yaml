namespace:
  name: "sample-app"
  create: true
  labels: {}
  annotations: {}

istio:
  injection:
    enabled: true
    labelKey: "istio-injection"
    labelValue: "enabled"

managedBy: "spectrocloud"
