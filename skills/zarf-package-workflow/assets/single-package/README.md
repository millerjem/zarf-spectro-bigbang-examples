# Example Zarf Package

Replace this example content with the package purpose, component matrix,
variable definitions, source provenance, and supported deployment targets.

```sh
./scripts/validate.sh
./scripts/build.sh
./scripts/publish.sh
```

The scripts default to `amd64` and require an explicit registry login before
publication. Treat published package versions as immutable.
